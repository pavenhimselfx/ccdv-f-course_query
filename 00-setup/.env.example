# Copy this file to ".env" and fill in your real key:
#   cp .env.example .env
#
# Do NOT commit the real ".env" file to version control — make sure it is
# listed in your .gitignore. Only this ".env.example" template (with no real
# secret in it) should ever be committed.

ANTHROPIC_API_KEY=your-key-here
